{% extends 'layout.html.twig' %}

{% block content %}
    <section class="text">
        <h1 class="mdl-typography--display-3">Zoznam cyklotrás</h1>
        <ul class="tracks mdl-list">
            {% for track in tracks %}

                <li class="track mdl-list__item">
            <span class="mdl-list__item-primary-content">
                <i class="mdl-list__item-icon material-icons">directions_bike</i>
                <a href="?p={{ track.basename }}">{{ track.name }}</a>
            </span>
                </li>

            {% endfor %}
        </ul>
    </section>
{% endblock %}
