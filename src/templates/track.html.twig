{% extends 'layout.html.twig' %}

{% block content %}

    {{ track.description|raw }}

    {% if track.gpx %}
    <p><strong>GPX súbor:</strong> <a href="{{ track.gpx }}">{{ track.gpx_name }}</a></p>
    {% endif %}

    {% if track.images %}
        {% for image in track.images %}
            <img class="img" src="{{ image }}" />
        {% endfor %}
    {% endif %}

{% endblock %}
